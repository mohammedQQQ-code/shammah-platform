'use client';\n\nimport Link from 'next/link';\nimport { useState } from 'react';\n\nexport default function Sidebar() {\n  const [isOpen, setIsOpen] = useState(true);\n\n  const menuItems = [\n    { href: '/dashboard', label: 'لوحة التحكم', icon: '📊' },\n    { href: '/materials', label: 'المواد الدراسية', icon: '📚' },\n    { href: '/quizzes', label: 'الاختبارات', icon: '📝' },\n    { href: '/ai-tutor', label: 'المعلم الذكي', icon: '🤖' },\n    { href: '/profile', label: 'الملف الشخصي', icon: '👤' },\n  ];\n\n  return (\n    <aside className={`${isOpen ? 'w-64' : 'w-20'} glass transition-all duration-300 p-4`}>\n      <div className=\"mb-8\">\n        <button\n          onClick={() => setIsOpen(!isOpen)}\n          className=\"text-2xl mb-4\"\n        >\n          ☰\n        </button>\n        {isOpen && <h1 className=\"text-xl font-bold gradient-text\">شمه</h1>}\n      </div>\n\n      <nav className=\"space-y-2\">\n        {menuItems.map((item) => (\n          <Link\n            key={item.href}\n            href={item.href}\n            className=\"flex items-center gap-3 p-3 rounded-lg hover:bg-green-700 transition-colors\"\n          >\n            <span className=\"text-2xl\">{item.icon}</span>\n            {isOpen && <span className=\"font-medium\">{item.label}</span>}\n          </Link>\n        ))}\n      </nav>\n    </aside>\n  );\n}\n